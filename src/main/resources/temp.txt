for(int i = 0; i < arr.length; i++) {
            if(arr[i] == '0') {
                currCount++;
            } else {
                if(!foundOne) {
                    currCount = 0;
                } else {
                    if(maxCount < currCount) {
                        maxCount = currCount;
                        currCount = 0;
                    }
                }
                foundOne = true;
            }
        }